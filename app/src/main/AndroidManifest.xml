<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cty.administrator.demo1">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IntentTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".RelationActivity"></activity> -->
        <!-- <activity android:name=".goodActivity"></activity> -->
        <!-- <activity android:name=".AutoCompleteTextView"></activity> -->
        <activity android:name=".goodActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RelationActivity" />
        <activity android:name=".AutoCompleteTextViewActivity" />
        <activity android:name=".MultiAutoCompleteTextViewActivity" />
        <activity android:name=".ButtonActivity" />
        <activity android:name=".Button2Activity" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".TotleButtonActivity" />
        <activity android:name=".CheckBoxActivity" />
        <activity android:name=".RadioButtonActivity" />
        <activity android:name=".SecondeTextActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".IntentTest2Activity"></activity>
    </application>

</manifest>